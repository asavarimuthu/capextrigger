{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm start",
      "name": "Capextrigger",
      "request": "launch",
      "type": "node-terminal",
      "cwd":"${workspaceFolder}//Capextrigger"
    },
    {
      "name": "cds serve",
      "request": "launch",
      "type": "node",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "cds",
      "args": [
        "serve",
        "--with-mocks",
        "--in-memory?"
      ],
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
